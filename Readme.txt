To run the application:

- create DB on your Sql Server instance, for example 'TransactionDB';
- run the table creation script in the DbScripts folder;
- modify the connection string in the Web.config file.